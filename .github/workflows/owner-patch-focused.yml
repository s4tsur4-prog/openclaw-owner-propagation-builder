# Legacy full pnpm-check workflow retired; manual no-op only.
name: Retired Owner candidate focused repair
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  legacy-retired:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo 'Retired. Use owner-focused.yml. No verification executed.'
